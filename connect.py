#!/usr/bin/python
#
# Connect script for INDI Telescope Scripting Gateway
#
# Arguments: none
# Exit code: 0 for success, 1 for failure
#

import sys
import time
from socket import *

import os 
#os.system('python /usr/share/indi/scripts/mount.py start')

#time.sleep(2)
#s = socket(AF_INET, SOCK_STREAM) #utworzenie gniazda
#s.connect(('localhost', 5555)) # nawiazanie polaczenia
#s.send("1 0 90") #odbior danych (max 1024 bajtow)
#s.close()
#coordinates = open('/tmp/indi-status', 'w')
#coordinates.truncate()
#coordinates.write('1 0 90')
#coordinates.close()

